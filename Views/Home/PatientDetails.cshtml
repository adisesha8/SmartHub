@model IEnumerable<ModelProject.ModelClass>
@using GridMvc.Html

<h3>@ViewBag.Message</h3>

<!--
<div>
@if (Model != null && Model.Count()>0)
    {
        <div class="panel panel-primary pre-scrollable">
            @Html.Grid(Model).Columns(columns =>
           {
               columns.Add(col => col.FirstName).Titled("First Name");
               columns.Add(col => col.LastName).Titled("Last Name");
               columns.Add(col => col.Sex).Titled("Gender");
               columns.Add(col => col.MaritalStatus).Titled("Marital Status");
           }).WithPaging(4).Sortable())
        </div>
    }
</div>

-->

<div>
    @if (Model != null && Model.Count() > 0)
    {
        <div class="panel panel-primary pre-scrollable">
            @Html.Grid(Model).Columns(columns =>
            {
                columns.Add(col => col.FirstName).Titled("First Name");
                columns.Add(col => col.LastName).Titled("Last Name");
                columns.Add(col => col.Sex).Titled("Sex");
                columns.Add(col => col.MaritalStatus).Titled("Address");
            }).WithPaging(4).Sortable()
        </div>
    }

</div>